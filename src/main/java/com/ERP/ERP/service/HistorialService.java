package com.ERP.ERP.service;
import org.springframework.stereotype.Service;


@Service
public class HistorialService {

/*
        sesion.setFechaInicio(LocalDateTime.now());

    // Set fechaCierre based on the configured duration
        sesion.setFechaCierre(sesion.getFechaInicio().plus(sessionDurationHours, ChronoUnit.HOURS));
        sesion.setObservacion(observacion);
        sesion.setDiagnostico(diagnostico);
        sesion.setHistorial(historial);

        return sesionRepository.save(sesion);

*/
}
